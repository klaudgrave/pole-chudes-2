# Acceptance

- Main flow is represented as an explicit transition table or equivalent typed state machine before any large runtime rewrite.
- Every stage branch has a parity case or a capture-backed confirmation.
- Flow tests cover bankrupt, skip, plus-letter, multiplier, prize accept/refuse, wrong solve, repeated-word avoidance, all-players-eliminated handling, and top-8 insertion.
