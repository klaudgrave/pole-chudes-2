# Acceptance

- `defaultAssetSpec` matches the DOS executable metadata and the ImHex templates.
- Parser tests pass against the original asset fixtures.
- Any format change or new binary fact updates both `web/src/spec/defaultSpecs.ts` and this folder before runtime code changes.
- No asset binary is modified in place.
